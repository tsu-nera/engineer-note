<% provide(:title, @user.name) %>
<% provide(:login, logged_in?) %>
<% provide(:user_id, current_user_id) %>

<%= react_component('User', {gravatar: gravatar_for(@user), name: @user.name, user_id: @user.id, microposts: feed_data(@microposts)}) %>
