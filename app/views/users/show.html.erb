<% provide(:title, @user.name) %>
<% provide(:login, logged_in?) %>
<% provide(:user_id, current_user_id) %>

<%= react_component('User', {gravatar: gravatar_for(@user, {size: 50}), name: @user.name, user_id: @user.id, microposts: @microposts}) %>
